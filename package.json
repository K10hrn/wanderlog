{
  "name": "wanderlog-tests",
  "version": "1.0.0",
  "description": "E2E test suite for Wanderlog",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "dotenv": "^16.4.5"
  }
}
